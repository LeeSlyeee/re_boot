diff --git a/Dev/Phase3_Architecture_Migration.md b/Dev/Phase3_Architecture_Migration.md
index ab640f7..fc7f7e8 100644
--- a/Dev/Phase3_Architecture_Migration.md
+++ b/Dev/Phase3_Architecture_Migration.md
@@ -164,3 +164,8 @@ if target_triggered == 'quiz':
 2. **마이크 클라이언트 중복 실행(Auto-Start) 버그 픽스**:
    - 기존에는 `rpi-status` API가 호출될 때마다 CMD 창이 뜨도록 구현되어 있어, STT 등 다른 동작 중에도 중복 실행되는 현상이 있었습니다.
    - 이를 해결하기 위해 프론트엔드(`LectureDetailView.vue`)에서 교수가 명시적으로 '연결 테스트' 버튼을 눌렀을 때만 `manual_launch: true` 플래그를 넘기도록 수정하여 불필요한 중복 자동 실행을 차단했습니다.
+3. **라즈베리 파이 단말기 발 웹훅 전송 실패(Timeout) 버그 픽스**:
+   - **증상**: 라즈베리 파이에서 KWS를 성공적으로 감지했음에도 불구하고, `❌ 웹훅 전송 실패 (서버 접속 불가): timed out` 에러가 발생하며 서버로 알림을 주지 못했습니다.
+   - **원인 1. 수신처 IP 설정 오류**: 라즈베리 파이 파이썬 스크립트(`server_pi_socket_VAD.py`)의 `DJANGO_WEBHOOK_URL`이 `127.0.0.1`로 하드코딩되어 있어, 라즈베리가 외부의 장고 서버가 아닌 라즈베리 기기 내부에서 웹 서버를 찾으려다 접속에 실패했습니다. (소켓 연결 시 받아오는 클라이언트의 실제 IP인 `addr[0]`를 추출하여 동적으로 URL을 생성하도록 코드를 수정해 해결했습니다.)
+   - **원인 2. 백엔드(Django) 서버의 로컬호스트(localhost) 쇄국 정책**: PC에서 프론트엔드(웹 브라우저)는 장고 서버에 정상 접근이 가능하여 착각하기 쉬우나, 장고 서버 구동 시 `python manage.py runserver`를 인자 없이 실행하면 기본값인 `127.0.0.1:8000`으로 켜집니다. 이는 "내 PC 안에서 출발한 접속만 허용하겠다"는 뜻이므로, 외부 네트워크 기기인 라즈베리 파이가 문을 두드려도 장고 서버가 무시해 버립니다.
+   - **최종 해결**: `start_all.bat` 파일 내의 백엔드 실행 스크립트를 `python manage.py runserver 0.0.0.0:8000`으로 변경하여 모든 외부 IP의 접근을 다 허용하도록 통신망 포트를 개방함으로써 웹훅 연동을 성공적으로 마무리했습니다.
diff --git a/Professor_dashboard/src/views/LectureDetailView.vue b/Professor_dashboard/src/views/LectureDetailView.vue
index 2228896..7c3fab8 100644
--- a/Professor_dashboard/src/views/LectureDetailView.vue
+++ b/Professor_dashboard/src/views/LectureDetailView.vue
@@ -1741,7 +1741,7 @@ onMounted(fetchDashboard);
                         <input v-model="rpiHost" placeholder="IP (예: 172.16.206.43)" class="rpi-input" />
                         <input v-model="rpiPort" placeholder="포트" class="rpi-input rpi-port" />
                         <button class="rpi-btn rpi-btn-test" @click="connectRpi" :disabled="rpiConnecting">
-                            {{ rpiConnecting ? '테스트 중...' : '연결 테스트' }}
+                            {{ rpiConnecting ? '테스트 중...' : '연결' }}
                         </button>
                     </div>
                 </div>
diff --git a/backend/learning/live_views.py b/backend/learning/live_views.py
index a44cdc4..7f5beec 100644
--- a/backend/learning/live_views.py
+++ b/backend/learning/live_views.py
@@ -134,6 +134,11 @@ class LiveSessionViewSet(viewsets.ViewSet):
             # 활성 퀴즈 비활성화
             session.quizzes.filter(is_active=True).update(is_active=False)
 
+            # [편의성 개선] 수업 종료 시 교수 PC에 띄워둔 KWS 마이크 CMD 창 자동 강제 종료
+            import os
+            # KWS_Mic_Client라는 창 타이틀을 가진 cmd.exe 프로세스를 찾아 종료
+            os.system('taskkill /FI "WINDOWTITLE eq KWS_Mic_Client*" /T /F >nul 2>&1')
+
             # 통합 노트 생성 시작 (비동기) — 중복 방지
             existing_note = LiveSessionNote.objects.filter(live_session=session).first()
             if not existing_note:
diff --git a/start_all.bat b/start_all.bat
index c201030..6601cd8 100644
--- a/start_all.bat
+++ b/start_all.bat
@@ -1,5 +1,5 @@
 @echo off
-start "Backend" cmd /k "cd backend && venv\Scripts\python manage.py runserver"
+start "Backend" cmd /k "cd backend && venv\Scripts\python manage.py runserver 0.0.0.0:8000"
 start "Frontend" cmd /k "cd frontend && npm run dev"
 start "Dashboard" cmd /k "cd Professor_dashboard && npm run dev"
 echo All services started!
